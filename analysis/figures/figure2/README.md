# Figure 2 — 10x dopaminergic interneuron coding across TH and DAT

**Claim:** awake TH and DAT glomerular populations differ in signed odor
specificity and reliable representational separation.

Primary panels: example maps; excitation/suppression breadth and lifetime
sparseness; threshold-free participation and E/S balance; integrated spatial
crossnobis RDMs. Cosine distance is the gain-insensitive secondary geometry.
Dimensionality is deliberately omitted. Mixture-focused and temporal geometry
are computed in the same framework but become the transition to the state
figure.

Thy1 is deliberately excluded; principal-neuron mixture discriminability is
handled in Figure 4.

Run `Figure2_awake_glomerular_coding.ipynb`; outputs go in `outputs/`.
